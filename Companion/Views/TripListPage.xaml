<?xml version="1.0" encoding="UTF-8" ?>
<ContentPage
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    x:Class="Companion.Views.TripListPage">

<NavigationPage.TitleView>
    <StackLayout Orientation="Horizontal" HorizontalOptions="Center">
            <Image Source="performance_icon.png" />
            <Label Text="Performance Page"/>
    </StackLayout>
</NavigationPage.TitleView>

    <ContentPage.Content>

        <StackLayout>
             <Frame BackgroundColor="DarkGray" Padding="24" CornerRadius="0">
                <Label Text="Select a Trip:" VerticalOptions="Center" HorizontalOptions="Center" />
            </Frame>

            <ListView
                x:Name="listView"
                BackgroundColor="White"
                HorizontalOptions="FillAndExpand"
                ItemTapped="Trip_ItemTapped"
                VerticalOptions="FillAndExpand">

                <ListView.ItemTemplate>
                    <DataTemplate>
                        <TextCell
                            Detail="{Binding Id}"
                            Text="{Binding Name}"
                            TextColor="Black" />
                    </DataTemplate>
                </ListView.ItemTemplate>

            </ListView>

        </StackLayout>
        
    </ContentPage.Content>
</ContentPage>
